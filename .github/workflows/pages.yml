name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

<<<<<<< HEAD
=======
# Sets permissions for GitHub Pages deployment
>>>>>>> origin/main
permissions:
  contents: read
  pages: write
  id-token: write

<<<<<<< HEAD
concurrency:
  group: "pages"
  cancel-in-progress: false
=======
# Allow one concurrent deployment
concurrency:
  group: "pages"
  cancel-in-progress: true
>>>>>>> origin/main

jobs:
  build:
    runs-on: ubuntu-latest
<<<<<<< HEAD
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
=======
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Set up Python
>>>>>>> origin/main
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
<<<<<<< HEAD

      - name: Install dependencies
        run: |
          pip install -r requirements.txt

      - name: Install Playwright
        run: |

      - name: Validate Golden Rule hours
        run: |
          python scripts/validate_golden_rule.py

      - name: Build MkDocs site
        run: |
          mkdocs build

      - name: Upload artifact
=======
      
      - name: Install Python dependencies
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt
      
      - name: Install Playwright (required for PDF generation)
        # Only install Playwright on pushes to main, not on PRs
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: |
          playwright install chromium
          playwright install-deps chromium
      
      - name: Validate Golden Rule hours
        run: |
          python scripts/validate_golden_rule.py
          echo "Golden Rule validation passed!"
      
      - name: Build MkDocs site with PDF (main branch)
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: |
          mkdocs build
          echo "MkDocs build completed successfully!"
      
      - name: Build MkDocs site without PDF (PR)
        if: github.event_name == 'pull_request'
        run: |
          mkdocs build -f mkdocs-pr.yml
          echo "MkDocs build completed successfully (PR mode, no PDF)!"
      
      - name: Verify PDF was generated
        # Only verify PDF on pushes to main
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: |
          if [ -f site/downloads/curriculum.pdf ]; then
            echo "PDF generated successfully!"
            ls -lh site/downloads/curriculum.pdf
          else
            echo "ERROR: PDF was not generated!"
            exit 1
          fi
      
      - name: Upload artifact for Pages
>>>>>>> origin/main
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./site

  deploy:
<<<<<<< HEAD
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
=======
    # Only deploy on push to main, not on PRs
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    
    needs: build
    runs-on: ubuntu-latest
    
    permissions:
      pages: write
      id-token: write
    
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    
>>>>>>> origin/main
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
